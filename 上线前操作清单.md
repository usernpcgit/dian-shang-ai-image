# 上线前，你一步步怎么做（大白话版）

按顺序做就行，不用管专业名词。

## 第 1 步：把做好的改动存上去并发布
- 打开 **GitHub Desktop**。
- 左边会看到几个改动的文件（`access.py`、`gentoken.py` 是新增的，`proxy.py`、`standalone.html` 是改过的）。
- 在左下角写一句说明，比如「加上访问码和双支付」，点 **Commit**。
- 再点 **Push**（推送到 main）。
- 这一步做完，Render 会自动重新发布你的网站，不用你手动去部署。

## 第 2 步：去 Render 后台设一个「密码钥匙」
- 打开 Render 网站，进你的 **self-ai-image** 服务。
- 点 **Environment（环境变量）**，加一项：名字填 `ACCESS_SECRET`，值填一段你自己随便编的长串字符（比如 30 位乱码）。
- 点保存。
- **记住这串字符**——第 3 步要用同样的它来生成访问码。
- 为啥要这步：现在网站靠代码里一个默认的钥匙在拦人，谁都能照着编，必须换成你自己的才安全。

## 第 3 步：给测试用户发访问码
- 在你电脑的项目文件夹里打开终端，运行：
  ```
  ACCESS_SECRET=你刚才那串字符 python gentoken.py --days 7 --note "测试用户A"
  ```
- 它会吐出一串码，把这串码发给对应的朋友/测试用户就行。
- 码 7 天有效，想发多久就改 `--days` 后面的数字。

## 第 4 步：确认落地页上的信息是你的
- 打开 `landing.html`，检查顶部的 `CONFIG`：价格对不对、邮箱是不是你的、微信/支付宝收款码图片路径对不对。
- 收款码图片（`ali-qr.png`、`wx-qr.png`）要确保已经放进 `assets` 文件夹并提交（第 1 步会一起提交）。

## 第 5 步：等发布完，自己先试一遍
- 回到 Render，看构建日志没报错、状态变成「可用」。
- 浏览器打开你的网站地址：
  - 首页能正常看；
  - 点进工具页（`/tool`），应该弹出「请输入访问码」；
  - 乱输一个 → 进不去；
  - 输你第 3 步生成的真码 → 能进，能正常生图。
- （可选）直接用接口试：不带码去生图 → 应该被拒绝。

## 第 6 步：加个「挂了告诉我」的提醒（可选但建议）
- 去 **UptimeRobot** 免费注册，新建一个监控，填你的地址 + `/health`，每 5 分钟检查一次，失败发邮件提醒你。
- 这样网站哪天挂了你能第一时间知道。

## 第 7 步：出问题怎么退回
- 万一发布后不对：进 Render 的 **Deploys** 页面，找到上一次正常的版本，点 **Rollback** 就能一键退回。
- 代码层面也能退：本地切回 `v1.0-launch` 标签重新推一次。

---

## 一句话记住
GitHub Desktop 提交推送 → Render 设 ACCESS_SECRET → 本地跑 gentoken 发码 → 核对落地页信息 → 等发布完自测 → 加个掉线提醒 → 出问题 Rollback。
