# 桌面端「安装后打不开」——根因定位与修复

## 结论先行
Mac / Windows 桌面端"装完打不开"不是单一 bug，而是**未签名侧载的固有限制 + 一个真实的启动逻辑缺陷**叠加。已全部修复，且只动了 `electron/` 下的桌面端文件，未碰共用的 `proxy.py`。

## 根因（实测确认，非猜测）
| 平台 | 现象 | 根因 |
|---|---|---|
| macOS | 双击被拦 / 一闪退出 | ① 未签名 app 从浏览器下载带 `com.apple.quarantine`，Gatekeeper 在 main.js 跑起来前就拦（运行时 `xattr -cr` 来不及）；② 即便右键放行，系统 `/usr/bin/python3`(3.9.6)**没有 `requests`**，`proxy.py` 启动即 `import requests` 崩溃 → `/health` 永不响应 → 超时弹"启动失败"退出 |
| Windows | 弹窗/起不来 | SmartScreen 拦截（需点"仍要运行"）；PyInstaller 的 `proxy-bin.exe` 常被 Defender 误删，无 Python 时后端起不来 |

实测：本机 `/usr/bin/python3 -c "import requests"` → `ModuleNotFoundError`；装上 requests 后 `DESKTOP_MODE=1` 起 `proxy.py`，`/health=ok`、`/tool` HTTP 200、stderr 干净 → 证明依赖就绪后后端链路完全正常。

## 改动清单
1. **`electron/main.js`**（核心修复）
   - `findPython()` 现优先选「版本达标且已装 `requests`」的解释器；
   - 新增 `ensureRequests()`：用 `python -m pip install` →（PEP668 外部管理时）`--break-system-packages` →（pip 缺失时）`python -m ensurepip` 自动补齐；
   - **关键**：装不上**不再**强行 spawn 必崩的后端，改为 `showLaunchHelp()` 弹出含「清隔离标记 / 右键打开 / 装 Python」步骤的提示后退出（原本会静默崩溃循环）。
2. **`electron/guide.html`**：修正"ZIP 不带隔离标记"误导（浏览器下载 zip 同样带 quarantine），改为强调**右键→打开**一次 + `curl` 下载避坑提示。
3. **`electron/install-helper.command`**：搜索路径加 `~/Downloads`；找不到时给出手动 `xattr -dr` 命令，并提示本助手自身也需右键打开。
4. **`桌面端安装说明.md`**（新增）：mac/win 一键命令、为什么打不开、开发者备注。

## 用户侧怎么解决（无需重签 $99）
- **macOS**：右键 App →「打开」一次（加入白名单，以后双击即开）；或终端 `xattr -dr com.apple.quarantine "/Applications/电商AI生图.app"`；确保有带 `requests` 的 Python（应用会自动装，装不上会明确提示）。
- **Windows**：SmartScreen 弹窗点「更多信息 → 仍要运行」；确保装了 Python 3.8+（勾 Add to PATH）。

## 验证
- `node --check electron/main.js` ✅ 通过，无 `checkPythonDeps`/`installPythonDeps` 残留引用；
- 后端链路实测 `/health`、`/tool` 正常；
- 本地 `electron/dist/mac-arm64` 为空仅是沙箱无缓存 Electron 二进制/无网络所致，非发布包问题。
