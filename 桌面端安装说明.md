# 生图Agent · 桌面端安装与「打不开」解决说明

免费侧载版（未向 Apple / Microsoft 付费签名），所以首次打开需要你手动放行一次。
下面按系统说明，照做即可正常启动。

---

## 一、macOS（最常见「打不开」在这里）

### 为什么会打不开？
从 GitHub Releases 用浏览器下载的 `.dmg` / `.zip`，系统会自动打上**隔离标记（quarantine）**。
未签名 App 带隔离标记时，双击会被 Gatekeeper 拦掉，提示：
- 「“生图Agent” 无法验证开发者，无法打开」
- 或 「“生图Agent” 已损坏，无法打开。应将其移到废纸篓」

> 注意：这不是真的损坏，是签名校验拦截。

### 解决办法（任选其一，只需做一次）

**方法 1（最省事）：右键 → 打开**
1. 在「应用程序」/「桌面」里找到「生图Agent.app」
2. **右键**点它 → 选「打开」
3. 弹出警告里再点一次「打开」
4. 之后就加入白名单，以后双击直接开 ✅

**方法 2（命令行，最彻底）：清掉隔离标记**
打开「终端」，执行（把路径换成你的实际位置）：
```bash
xattr -dr com.apple.quarantine "/Applications/生图Agent.app"
open "/Applications/生图Agent.app"
```
如果 App 在桌面：把 `/Applications/` 改成 `~/Desktop/`。

**方法 3（下载阶段就避坑）：用终端 curl 下载**
浏览器下载才会加隔离标记；用终端下载则不会：
```bash
curl -L -o 生图Agent.zip "<发布页里的 zip 链接>" && unzip 生图Agent.zip
```
解压后双击即可直接打开。

### 还有一种「打不开」：后端起不来（窗口一闪/报错退出）
应用依赖系统 Python 运行内置代理。若你的 Mac 只有命令行工具自带的 Python（没有 `requests` 模块），
新版本会在启动失败时**弹出明确的解决提示**，并自动尝试安装所需模块。若仍失败：
- 去 https://www.python.org/downloads/mac-osx/ 装 Python 3.8+（默认装到 `/usr/local/bin/python3`）
- 重开应用即可，应用会再次自动补齐依赖

> 辅助脚本 `electron/install-helper.command` 也能一键清标记并启动；若它自己也打不开，请右键该文件 →「打开」。

---

## 二、Windows

### SmartScreen 拦截（正常现象）
首次安装/运行会弹出 **「Windows 保护了你的电脑」(SmartScreen)**：
1. 点 **「更多信息」**
2. 点 **「仍要运行」**
这不是病毒提示，是 Windows 对未签名应用的标准拦截。之后即可正常启动。

### 后端起不来（提示「代理启动失败」）
桌面端优先用**系统 Python** 启动后端；若没装 Python，会回退到内置 `proxy-bin.exe`。
- 请安装 Python 3.8+（https://www.python.org/downloads/windows/），**务必勾选「Add to PATH」**
- 若杀毒软件（尤其 Windows Defender）误删了 `proxy-bin.exe`，请把应用目录加入白名单，或确保已装系统 Python

---

## 三、给打包/发布的同学（开发者备注）

- Mac 构建在 CI 的 `build-mac` 中**故意不打包 proxy-bin**、且**不做 codesign**：
  - 未签名 App 只触发 Gatekeeper，用户右键→打开即可放行；
  - 若对 `.app` 做 `codesign --deep --force --sign -`，会覆盖 Electron 框架的原始 Apple 签名，反而可能触发 XProtect 把整个 App 当恶意软件删掉。
- 真正的「放行」只能由用户侧完成（清 quarantine 或右键→打开），打包阶段无法绕过（下载时才加隔离标记）。
- `main.js` 现在会：优先选带 `requests` 的 Python；缺模块时用 `python -m pip` / `ensurepip` 自动装；
  装不上**不再**强行启动必崩的后端，而是弹含上述步骤的提示。
